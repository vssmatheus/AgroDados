export class Localidade {
    constructor(id, nome_agricultor, nome_propriedade, cultura, talhao){
        this.id = id;
        this.nome_agricultor = nome_agricultor;
        this.nome_propriedade = nome_propriedade;
        this.cultura = cultura;
        this.talhao = talhao;
    }
}

export default Localidade;